//endpoints.js inside the composable folder


const endpoints = {
    createEvent: '/events',
    eventList: '/events/list',
    loginUser:'/users/login',
    signupUser:'/users/signup'
    
  
// Define more endpoints here if needed
};

export { endpoints };
